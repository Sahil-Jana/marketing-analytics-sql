# Legacy Reference Files

These files are the original reference project this repository was
redesigned from (basic/template-style SQL, no reproducible schema or
dataset, segmentation based on an ungrounded `churn_probability` column).
They are kept for historical reference only.

They are **not** part of the current architecture. See the root
`README.md` and the `sql/` directory for the current, validated project.
